# Light runtime core — required for the offline compound system + factory.
# (Prefer `pip install -e .` which reads pyproject.toml; this file mirrors it.)
pyyaml>=6.0
pydantic>=2.0
numpy>=1.24

# --- Optional extras (install only what you need) ---
# API (pip install -e '.[api]'):
#   fastapi>=0.110
#   uvicorn>=0.29
#   httpx>=0.27
# Real reasoning core / training (pip install -e '.[ml]'):
#   torch
#   transformers
#   peft
#   trl
#   accelerate
# Retrieval quality (pip install -e '.[retrieval]'):
#   sentence-transformers
#   faiss-cpu
# Compression / export (pip install -e '.[compress]'):
#   bitsandbytes
#   auto-gptq
#   autoawq
#   onnx
#   onnxruntime
#   coremltools      # optional CoreML export
#   tensorflow       # optional TFLite export
# Dev (pip install -e '.[dev]'):
#   pytest>=8
#   ruff>=0.5
#   httpx>=0.27
